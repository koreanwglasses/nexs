{
  "workspaces": [
    "examples/*",
    "packages/*"
  ],
  "scripts": {
    "build": "yarn workspace @koreanwglasses/nexs-core build && yarn workspace @koreanwglasses/nexs build && yarn workspace @koreanwglasses/nexs-server build"
  },
  "packageManager": "yarn@3.1.1",
  "devDependencies": {
    "typescript": "^4.5.2"
  }
}
